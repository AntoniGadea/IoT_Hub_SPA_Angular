import { MoreInfoDirective } from './more-info.directive';

describe('MoreInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new MoreInfoDirective();
    expect(directive).toBeTruthy();
  });
});
