import { SearchPanelPipe } from './search-panel.pipe';

describe('SearchPanelPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPanelPipe();
    expect(pipe).toBeTruthy();
  });
});
