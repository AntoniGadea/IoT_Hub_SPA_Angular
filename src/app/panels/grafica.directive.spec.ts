import { GraficaDirective } from './grafica.directive';

describe('GraficaDirective', () => {
  it('should create an instance', () => {
    const directive = new GraficaDirective();
    expect(directive).toBeTruthy();
  });
});
