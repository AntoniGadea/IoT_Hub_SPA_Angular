import { SearchLightPipe } from './search-light.pipe';

describe('SearchLightPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchLightPipe();
    expect(pipe).toBeTruthy();
  });
});
